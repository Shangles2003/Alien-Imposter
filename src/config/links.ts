/**
 * Public URLs for legal pages and support.
 *
 * These pages live in the repo under `website/` — host them (GitHub Pages,
 * Netlify, Vercel, anything static) and update the BASE below to match.
 * The same URLs go into App Store Connect (Support URL / Privacy Policy URL).
 */
const BASE = 'https://torna.github.io/alien-imposter';

export const LINKS = {
  privacyPolicy: `${BASE}/privacy.html`,
  termsOfService: `${BASE}/terms.html`,
  support: `${BASE}/support.html`,
  marketing: `${BASE}/`,
  supportEmail: 'alienimpostergame@gmail.com',
};
